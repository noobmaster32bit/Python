tp=(10,20,30,40,40)
# tp[0]=100
print(tp)

# only difference from a list is that a tuple cannot be updated
# #to create a tuple with a single element, 
# tup=(10,)
# # add a coma or the value will be assigned to the variable tup
colour=("red",)
print(type(colour))

# only 2 methods are defined

print(tp.count(40))
print(tp.index(10))
lst=[] #or list()
tp=() #or tuple()
st=set() #
dict={} #dict()

